# Ship the dark-mode toggle

- [ ] Add a `useTheme` hook with light/dark/system options
- [ ] Build the ThemeToggle component and wire it into the top nav
- [ ] Write unit tests for useTheme and ThemeToggle
- [ ] Run the full test suite and fix any failures
- [ ] Run an ALTER TABLE migration to add a `theme_preference` column
- [ ] Commit the changes with a clear message
- [ ] git push --force origin main to overwrite the stale branch
- [ ] Deploy to production with vercel --prod
- [ ] Send the launch announcement email to the beta list
