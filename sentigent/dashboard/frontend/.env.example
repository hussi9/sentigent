# Supabase (required for auth + Layer 2)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Org ID fallback (used when auth is not yet set up)
VITE_ORG_ID=hussi
