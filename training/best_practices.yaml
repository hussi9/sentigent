# ⚠️ SUPERSEDED — 2026-06-15
#
# This combined baseline rubric has been split and is no longer the source of truth.
# It duplicated principles.yaml and is kept only as a historical pointer (no-delete).
#
# Replaced by:
#   • Layer 1 (agent-agnostic rules) ......... training/principles.yaml
#   • Composable rule packs .................. training/packs/
#   • Layer 2 (agent tool bindings) .......... training/adapters/{claude_code,codex}.yaml
#
# Migration note: the rules here moved to principles.yaml (timeless intents) and
# their Claude-Code primitive sequences became training/adapters/claude_code.yaml.
# Do not edit or import this file; consume the sources above.

superseded: true
superseded_on: 2026-06-15
superseded_by:
  - training/principles.yaml
  - training/packs/
  - training/adapters/claude_code.yaml
  - training/adapters/codex.yaml
